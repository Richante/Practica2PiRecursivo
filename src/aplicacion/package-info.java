/**
 * Proporciona la clase Principal para el cálculo del número PI mediante el método de Montecarlo.
 * Esta clase contiene el método principal (main) que utiliza la clase Matematicas para generar
 * una aproximación al número PI de manera recursiva.
 *
 * <p>La clase {@link aplicacion.Principal Principal} contiene el punto de entrada principal
 * (método main) que permite ejecutar la aplicación y obtener una aproximación al número PI
 * mediante el método de Montecarlo.</p>
 *
 * @since 1.0
 */
package aplicacion;
