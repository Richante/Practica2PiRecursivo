/**
 * Proporciona clases para el cálculo del número PI mediante el método de Montecarlo.
 * Este paquete incluye una implementación recursiva que utiliza la clase Matematicas
 * para generar aproximaciones al número PI.
 *
 * <p>La clase Principal contiene el método principal (main) que utiliza la clase Matematicas
 * para generar una aproximación al número PI mediante el método de Montecarlo.</p>
 *
 * @since 1.0
 */
package Practica2PiRecursivo;
